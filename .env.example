# Required — Google Gemini API key for graph extraction, chat, and search
GEMINI_API_KEY=your_api_key_here

# Optional — Override API key for langextract in the HCI Studio (defaults to GEMINI_API_KEY)
# LANGEXTRACT_API_KEY=

# Optional — Flask configuration
# FLASK_DEBUG=true
# PORT=5000
